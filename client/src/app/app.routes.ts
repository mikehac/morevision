import { Routes } from '@angular/router';
import { VehicleGridComponent } from './vehicle-grid/vehicle-grid.component';

export const routes: Routes = [{ path: '', component: VehicleGridComponent }];
